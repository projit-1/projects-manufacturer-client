import React from 'react';

const NotFound = () => {
    return (
        <div>
            <h2 className='text-3xl text-red text-center'>the page is not found</h2>
        </div>
    );
};

export default NotFound;